version: '3.8'

services:
  dashboard-finance:
    image: httpd:2.4-alpine
    container_name: rsv-dashboard-finance
    restart: unless-stopped
    ports:
      - '3010:80'
    volumes:
      - ./dashboard-finance/index.html:/usr/local/apache2/htdocs/index.html:ro
      - ./dashboard-finance/httpd.conf:/usr/local/apache2/conf/httpd.conf:ro
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost/']
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - rsv360-network

networks:
  rsv360-network:
    external: true
